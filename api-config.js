// TrueSight AI API Configuration
// For single-deploy projects (Vercel, single server), leave this empty ('') so the frontend communicates with the same origin.
// If running the frontend independently on a different port during local development, specify the backend URL (e.g. 'http://localhost:8000').
window.API_BASE_URL = window.API_BASE_URL || "";
